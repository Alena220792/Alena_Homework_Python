# Документация проекта

## Описание базового синтаксиса записи и форматирования

Проект использует следующие технологии и синтаксис:

1. **Python**: Основной язык программирования для написания тестов.
2. **Selenium**: Библиотека для автоматизации взаимодействия с веб-браузером.
3. **Pytest**: Фреймворк для написания и запуска тестов.
4. **Allure**: Инструмент для генерации отчетов о выполнении тестов.

### Форматирование кода

- Код форматируется в соответствии с PEP 8 (стиль написания кода на Python).
- Используются docstrings для документирования методов и функций.
- Все шаги теста размечаются с помощью `@allure.step` или `with allure.step` для улучшения читаемости отчетов.
- Используются декораторы `@allure.epic`, `@allure.feature`, `@allure.story`, `@allure.title`, `@allure.description`, `@allure.severity`  для максимальной прозрачности отчета.

--- 

## Инструкция по запуску тестов для формирования отчета Allure

1. Установите необходимые зависимости:
   ```bash
   pip install -r requirements.txt
   ```

2. Запустите тесты с генерацией отчета Allure:
   ```bash
   pytest --alluredir=./allure-results
   ```

   Эта команда запустит все тесты и сохранит результаты в директорию `./allure-results`.

---

## Инструкция по просмотру сформированного отчета Allure

1. После завершения тестов, чтобы просмотреть отчет, выполните команду:
   ```bash
   allure serve ./allure-results
   ```

   Эта команда запустит локальный сервер и откроет отчет в браузере.

2. В отчете Allure вы увидите:
2.1 Отчет калькулятора
   - **Название теста**: `Проверка медленного сложения 7 + 8`.
   - **Описание теста**: `Тест устанавливает задержку в 45 секунд, выполняет сложение и проверяет результат`.
   - **Шаги теста**:
     - Инициализация и открытие страницы.
     - Ввод значения задержки в поле #delay.
     - Поочередное нажатие кнопок 7, +, 8, =.
     - Явное ожидание (WebDriverWait) появления результата на экране.
     - Финальная проверка (assert) значения 15.
   - **Результат**: Успешное выполнение или ошибка с указанием ожидаемого и фактического результата.
2.2 Отчет магазина 
   - **Название теста**: `Проверка полной цепочки покупки и итоговой суммы`.
   - **Описание теста**: `Тест проходит авторизацию, добавляет товары в корзину, заполняет данные и сверяет финальную стоимость`.
   - **Шаги теста**:
     - Авторизация под standard_user.
     - Добавление трех конкретных товаров в корзину.
     - Переход в корзину и оформление (Checkout).
     - Заполнение формы данными покупателя (имя, фамилия, индекс).
     - Считывание итоговой стоимости (Total).
     - Проверка вхождения суммы $58.29 в итоговую строку.
   - **Результат**: Успешное выполнение или ошибка с указанием ожидаемого и фактического результата.